<?php if($this->getCurrencyCount()>1): ?>
    <div class="currency">
            <div class="btn-group">
                         <?php foreach ($this->getCurrencies() as $_code => $_name): ?>
                             <?php if($_code == $this->getCurrentCurrencyCode()): 
                                $currency_symbol = Mage::app()->getLocale()->currency($_code)->getSymbol();?>

                                <button class="dropdown-toggle" data-toggle="dropdown" id="dropdownCurrencySelected" href="#"><?php echo $_code ?> 
                                </button>
                            <?php endif; ?>                  
                        <?php endforeach; ?>
                <ul class="currencyList" id="currencyListview">
                    <?php foreach ($this->getCurrencies() as $_code => $_name): 
                        $currency_symbol = Mage::app()->getLocale()->currency($_code)->getSymbol();?>
                        <li>
                            <button onclick="location.href='<?php echo $this->getSwitchCurrencyUrl($_code)?>'">
                             <?php echo $_code ?>
                            </button>
                        </li>
                    <?php endforeach; ?>
                </ul>
            </div>
    </div>
<?php endif; ?>